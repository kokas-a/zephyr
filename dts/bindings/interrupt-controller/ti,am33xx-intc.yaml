description: TI AM33xx interrupt controller

compatible: "ti,am33xx-intc"

include: [interrupt-controller.yaml, base.yaml]

properties:
  "#interrupt-cells":
    const: 2

interrupt-cells:
  - irq
  - priority